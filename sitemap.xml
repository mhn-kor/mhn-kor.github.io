<?xml version="1.0" encoding="UTF-8"?>
<!-- 탭이 #해시로만 갈리므로 크롤러에게는 페이지가 하나입니다.
     검색엔진은 URL 의 # 뒤를 버리고 색인하기 때문에 #smelt · #build 를 여기 적어도
     전부 아래 한 줄과 같은 주소로 취급됩니다. 탭마다 검색에 걸리게 하려면
     /smelt/ 처럼 진짜 경로가 있어야 하고, 그건 라우팅을 바꿔야 합니다.
     lastmod 는 일부러 뺐습니다 — 손으로 관리하면 반드시 낡고, 낡은 값은 없느니만 못합니다. -->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://mhn-kor.github.io/</loc>
  </url>
</urlset>
